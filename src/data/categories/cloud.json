[
  {
    "id": "list-s3-buckets",
    "intent": "List s3 buckets",
    "aliases": [
      "aws s3 ls",
      "show s3 buckets",
      "list aws buckets",
      "get s3 buckets"
    ],
    "windows": "aws s3 ls",
    "linux": "aws s3 ls",
    "mac": "aws s3 ls",
    "description": "CLI for AWS S3 - provides storage through web services interfaces. Some subcommands such as `cp` have their own usage documentation.",
    "examples": [
      "aws s3 ls",
      "aws s3 ls s3://my-bucket",
      "aws s3 ls {{bucket_name}}",
      "aws s3 sync {{path/to/directory}} s3://{{bucket_name}}",
      "aws s3 sync s3://{{bucket_name}} {{path/to/target}}",
      "aws s3 sync {{path/to/directory}} s3://{{bucket_name}} --exclude {{path/to/file}} --exclude {{path/to/directory}}/*",
      "aws s3 rm s3://{{bucket}}/{{path/to/file}}",
      "aws s3 {{any_command}} --dryrun"
    ],
    "category": "cloud",
    "riskLevel": "safe"
  },
  {
    "id": "copy-to-s3-bucket",
    "intent": "Copy to s3 bucket",
    "aliases": [
      "aws s3 cp",
      "upload to s3",
      "download from s3",
      "s3 copy file"
    ],
    "windows": "aws s3 cp <source> s3://<bucket-name>",
    "linux": "aws s3 cp <source> s3://<bucket-name>",
    "mac": "aws s3 cp <source> s3://<bucket-name>",
    "description": "CLI for AWS S3 - provides storage through web services interfaces. Some subcommands such as `cp` have their own usage documentation.",
    "examples": [
      "aws s3 cp test.txt s3://my-bucket/",
      "aws s3 ls {{bucket_name}}",
      "aws s3 sync {{path/to/directory}} s3://{{bucket_name}}",
      "aws s3 sync s3://{{bucket_name}} {{path/to/target}}",
      "aws s3 sync {{path/to/directory}} s3://{{bucket_name}} --exclude {{path/to/file}} --exclude {{path/to/directory}}/*",
      "aws s3 rm s3://{{bucket}}/{{path/to/file}}",
      "aws s3 {{any_command}} --dryrun"
    ],
    "category": "cloud",
    "riskLevel": "safe"
  },
  {
    "id": "describe-ec2-instances",
    "intent": "Describe ec2 instances",
    "aliases": [
      "aws list ec2",
      "list virtual machines",
      "aws show instances",
      "describe ec2"
    ],
    "windows": "aws ec2 describe-instances",
    "linux": "aws ec2 describe-instances",
    "mac": "aws ec2 describe-instances",
    "description": "The official CLI tool for Amazon Web Services. Some subcommands such as `s3` have their own usage documentation.",
    "examples": [
      "aws ec2 describe-instances",
      "aws configure wizard",
      "aws configure sso",
      "aws sts get-caller-identity",
      "aws dynamodb list-tables --region {{us-east-1}} --output yaml",
      "aws iam create-user --cli-auto-prompt",
      "aws dynamodb wizard {{new_table}}",
      "aws dynamodb update-table --generate-cli-skeleton",
      "aws {{command}} help"
    ],
    "category": "cloud",
    "riskLevel": "safe"
  }
]